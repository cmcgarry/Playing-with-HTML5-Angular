/* All JavaScript code MUST be declared inside a closure wrapping the whole file and
this closure MUST be in strict mode.*/
(function () {
  "use strict";

  // All the JavaScript for this file.
  var app = angular.module('paths', [
      'ui.router',
      'ui.bootstrap'
  ]);

})();
